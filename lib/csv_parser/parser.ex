defmodule CSV.Parser do
  @moduledoc false

  def run(_data) do
  end

  # defp iso8601_to_unix_time(date_time) do
  #   {{year, month, day}, {hour, minute, second}} = :iso8601.parse(date_time)

  #   dt = %DateTime{year: year, month: month, day: day, zone_abbr: "UTC",
  #     hour: hour, minute: minute, second: second, microsecond: {0, 0},
  #     utc_offset: 0, std_offset: 0, time_zone: "Etc/UTC"}

  #   DateTime.to_unix(dt)
  # end
end
